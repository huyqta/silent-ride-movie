{
  "name": "silent-ride-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "deploy": "pnpm --dir apps/web deploy",
    "update-db": "echo 'Syncing types...'",
    "setup": "sh scripts/setup-hooks.sh"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "vercel": "^51.1.0"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "packageManager": "pnpm@10.32.1"
}
